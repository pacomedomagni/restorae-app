# Restorae App (Expo / React Native)

## Local development

1. Create env:
   - Copy `.env.example` → `.env`
2. Install + run:
   - `npm ci`
   - `npm run start`

## Quality gates

- Lint: `npm run lint`
- Tests: `npm test`
